# Documentação
aqui temos a documentação de todo o projeto

# API

Estrutura de pastas:
```
src/
├── config/          
├── controller/      
├── enum/            
├── middlewares/      
├── model/            
├── repositories/     
├── routes/           
├── services/         
└── view/             
```

## Rotas
### Usuarios
Todas as rotas estão funcionando em /api/users

`GET`: `/` Pega a informação de todos os usuarios, porém não mostra a senha

`GET`: `/:id` Pega a informação de um ususario pelo ID 

`PUT`: `/:id` Altera as infomaçoes do ususario pelo id
```

```

`DELETE`